# Known false positives, ignored by exact finding fingerprint
# (commit:path:rule:line). Fingerprints are deliberately narrow: they suppress
# one specific finding in one specific commit, and never a file, path or rule
# across the repo, so real secrets are still caught everywhere.
#
# Empty on purpose. The previous entry covered a high-entropy HMAC key in a
# test fixture, pinned to a commit that no longer exists: the history was
# rewritten when the project moved repositories, and the branch carrying that
# commit was not migrated. Verified by removing the file and rescanning -- 242
# commits, no findings.
