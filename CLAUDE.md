# CLAUDE.md

Follow the repo instructions in `./AGENTS.md`.

Operational notes:

- Run the commands listed in `AGENTS.md` for lint/test/build.
- Do not read secrets: `.env`, `.env.*`, `secrets/**`.
- Prefer minimal diffs; keep changes scoped.
